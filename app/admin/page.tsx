import PoolForm from './components/PoolForm';

const MarketPage = () => {
  return (
    <main className="py-4 mt-24 w-full flex items-center justify-center">
      <PoolForm />
    </main>
  );
};

export default MarketPage;
